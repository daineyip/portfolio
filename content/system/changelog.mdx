# Changelog

## 2026-08-27

- Added global search on `Cmd/Ctrl+K`, over labels, paths and document text
- Added `lib/search-index.ts`: documents stripped to plain text at build time
- Added a search field to the menu bar
- Added window pinning, halves and fill, by keyboard and by dragging to an edge
- Added a snap outline, instant on the halves and held on the top
- Added `Ctrl+Alt` shortcuts; wrote `shortcuts.mdx`, opened by `?`
- Added a pin glyph to pinned title bars and taskbar tabs
- Replaced the wallpaper greeting with an intro whose role, city and company expand
- Added a cursor that changes with what it is over
- Grew the traffic lights and their hit areas
- Added a favicon and page title
- Added a small-screen notice under 768px, until Boring Mode exists
- Added Vercel Analytics
- Fixed desktop layering: blocks, then the intro, then icons, then windows
- Dropped the address from the Inbox header
- Removed the late-night wallpaper note

## 2026-08-26

- Wrote readmes for MarketerHire, Binance.US, Onware, Dunroe, Oval Updates,
  HyperScreen, WorkInProgress and Systopia
- Added `image` and `video` node kinds with their own app templates
- Added `meta` on nodes: years now render under folder labels
- Reordered Projects newest-first
- Added PDFs, repo links and site links to the work and project folders
- Added `remark-gfm`; tables in MDX render now
- Stripped an `eXIf` chunk that was rotating `founders.png` at render
- Rewrote `prd.mdx`, removed `design-spec.mdx`, added `TODO.mdx`
- Added a late-night note to the wallpaper, 2am to 5am

## Unreleased

- MDX content pipeline, content tree, and information architecture.
